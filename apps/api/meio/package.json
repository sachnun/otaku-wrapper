{
  "name": "@api/meio",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "test": "vitest run"
  },
  "dependencies": {
    "@hono/swagger-ui": "^0.5.2",
    "@hono/zod-openapi": "^1.1.5",
    "@otaku-wraper/core": "workspace:*",
    "hono": "^4.10.7",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "vitest": "^4.0.15",
    "wrangler": "^4.4.0"
  }
}
