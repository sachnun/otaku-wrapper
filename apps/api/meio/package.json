{
  "name": "@api/meio",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "test": "vitest run"
  },
  "dependencies": {
    "@hono/swagger-ui": "^0.5.2",
    "@hono/zod-openapi": "^1.1.5",
    "@otaku-wrapper/core": "workspace:*",
    "hono": "^4.11.0",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "vitest": "^4.0.15",
    "wrangler": "^4.54.0"
  }
}
